node_modules/
*.jsonl

# Python (SDK de E5)
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# E12 · artefactos generados por QA (evidencia E2E e informes de aceptación)
tests/e2e/evidence/
acceptance/report.json

# Andamiaje de desarrollo local (scripts y datos del stack v2 en local)
*.local.mjs
.local/

# Config local del stack (puertos, dominio, perfil): no se versiona.
infrastructure/.env

# Cobertura de vitest (R2.1: se publica como artefacto de CI, no se versiona)
coverage/
